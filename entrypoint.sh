#!/bin/bash

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "🛠  ʟᴏᴀᴅɪɴɢ ᴘʟᴜɢɪɴs..."

count=0
for file in plugins/*.py; do
    if [ -f "$file" ]; then
        name=$(basename "$file")
        case "$name" in
            __*) ;;
            *)
                echo "🔄 ʟᴏᴀᴅᴇᴅ : $name"
                count=$((count+1))
                sleep 0.05
            ;;
        esac
    fi
done

echo "🎉 ᴛᴏᴛᴀʟ $count ᴘʟᴜɢɪɴs ɪᴍᴘᴏʀᴛᴇᴅ !"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo ""

python3 bot.py

